[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && bash build.sh"

[deploy]
startCommand = "uvicorn machine_learning_360v2.app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
